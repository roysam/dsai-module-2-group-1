
version: 2

models:
  - name: dim_products
    description: "Product dimenstion model contains information about products such as size, category, etc."
    config:
      schema: star
      materialized: table


  - name: dim_sellers
    description: "Seller dimenstion model contains information about sellers such as seller ID, city and state, etc."
    config:
      schema: star
      materialized: table
 
  - name: dim_customers
    description: "Customer dimenstion model contains information about customer such as customer ID, city and state, etc."
    config:
      schema: star
      materialized: table
